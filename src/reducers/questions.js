'use strict';

// Questions reducer, has questions has default state of an empty object {}.
const questions = (state = {}, action) => {
    switch(action.type) {
        default:
            return state;
    }
};